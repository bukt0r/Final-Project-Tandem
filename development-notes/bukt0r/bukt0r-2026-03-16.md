# Дата: 16.03.2026
Сегодня продолжил работу над базой вопросов. В файле src/data/questions.ts добавлен массив questions, который содержит 20 вопросов, все вопросы сгенерированы чатом GPT. 
Вопросы распределены по нескольким категориям: `JavaScript`, `React`, `TypeScript`, `Algorithms` и `CSS`. Также для каждого вопроса указана сложность (easy, medium, hard) и 
добавлены теги. Есть идея делать сопровождение на двух языках(eng/rus) - не знаю пока буду ли я этим заниматься, пока только русский. Далее был создан модуль для доступа к 
данным вопросов `src/entities/question/api/questionsApi.ts`. В нём реализованы основные методы для работы с вопросами:

getQuestions() — получение списка всех вопросов

getQuestionById() — получение вопроса по id

getQuestionsByCategory() — фильтрация вопросов по категории

searchQuestions() — поиск вопросов по тексту

Поиск реализован без учёта регистра, а при пустом запросе возвращается полный список вопросов. Все функции работают с mock-массивом `questions`


## Что было сделано:
- Добавлена mock база вопросов (src/data/questions.ts)
- Создано 20 вопросов по разным категориям
- Вопросы и ответы на русском языке (возможно будет 2 языка)
- Реализован API слой для работы с вопросами
- Добавлены функции получения, фильтрации и поиска вопросов

## Проблемы:
- Нужно было решить, на каком языке хранить вопросы — английском или русском
- При проектировании API слоя нужно было продумать, какие методы понадобятся дальше для фильтрации и поиска

## Решения:
- Было решено временно хранить вопросы на русском языке, чтобы упростить работу с интерфейсом
- API слой был реализован сразу с базовыми методами: получение списка, поиск и фильтрация
- Поиск сделан регистронезависимым для более удобной работы пользователя

## Планы:
- Реализовать страницу списка вопросов
- Создать компонент карточки вопроса (QuestionCard)
- Добавить отображение категории и сложности вопроса
- Реализовать компонент индикатора сложности (DifficultyBadge)