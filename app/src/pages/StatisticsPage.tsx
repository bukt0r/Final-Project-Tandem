import type { FC } from 'react'

const StatisticsPage: FC = () => {
  return (
    <section className="px-4 py-6">
      <h1 className="text-xl font-semibold text-app-text">Statistics</h1>
      <p className="mt-2 text-sm text-app-text-muted">Study statistics will be displayed here.</p>
    </section>
  )
}

export default StatisticsPage
